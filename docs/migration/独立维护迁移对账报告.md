# 独立维护迁移对账报告

> 对账时间：2026-08-18 11:32（Asia/Shanghai）  
> 结论：数据库独立运行和单一事实源切换完成；生产链路不再读取旧文件，已迁移领域的内容库副本已清零。

## 一、迁移结果

- 迁移从 0013 向前应用至 0014；0001–0013 未改写。
- 迁移前无 queued/running 作业、分析或回测。
- 0014 按固定 `legacy_path` 删除 7 份重复内容文档、7 个正文版本及对应导入证据：
  - `短线/标的池.md`
  - `长线/标的池.md`
  - `定时任务/每日交易计划.md`
  - `定时任务/周中短线检查.md`
  - `定时任务/每周评分.md`
  - `支撑/归档/持仓流水/2026年07月.md`
  - `支撑/归档/持仓流水/2026年08月.md`

删除后上述路径在 `content_document` 和 `content_legacy_import` 中均为 0。

## 二、当前内容与提示词

| 对账项 | 结果 |
|---|---:|
| 内容文档 / 版本 | 27 / 29 |
| 策略 | 3 |
| 指引 | 5 |
| 当前交易计划 | 2 |
| 交易计划归档 | 17 |
| 作业提示词 / 版本 | 3 / 6 |
| 作业定义 | 4 |
| 未绑定提示词的 agent_flow | 0 |
| 财务快照 / 估值快照 | 1 / 1 |
| 分析运行 | 1 |

内容库只保存策略、指引、当前交易计划和交易计划归档。当前领域事实映射为：

| 领域 | 唯一事实源 |
|---|---|
| 持仓、账户、持仓流水 | `portfolio_*` |
| 标的池 | `pool_membership` |
| Agent Flow 提示词 | `job_prompt*` |
| 作业状态、缺口与结果 | `job_definition` / `job_run` |
| 行情 | `market_bar` |
| 分析与估值 | `analysis_*` / `fundamental_*` / `valuation_*` |
| 回测 | `backtest_*` |

`content_legacy_import` 只保留其余迁移审计，不进入 Agent 轻量表索引，也不能作为业务事实查询。

## 三、内置 Agent

- 系统提示词已加入单一事实源映射和数据库化任务读取路由，覆盖市场、组合、已有持仓、新机会/评分、做 T/临时决策、关键位与历史复盘。
- 规则只读取 `content_document.current_revision_id` 指向的当前正文；通常只采用 `published`。目标日交易计划不得覆盖策略正文。
- 研究评分、量化条件、计划动作和运行结果必须分开；数据缺失必须列出缺口，策略外操作标记为“实盘例外”。
- 系统事实块新增最新 `portfolio_account_snapshot`，与当前持仓、今日作业、行情截止日和轻量表索引一起动态注入。
- 用户明确要求写入且存在领域工具时必须调用工具；确认制只表述为待确认提案，YOLO 只有成功后才表述为已写入。

## 四、可移植初始化包

- Payload：`bootstrap/stock_init_2026-08-18_111726.ndjson.gz`
- Manifest：`bootstrap/stock_init_2026-08-18_111726.manifest.json`
- 迁移上限：14
- 行情：320,881 行
- 内容版本哈希：29
- 作业提示词版本哈希：6
- 财务快照 / 估值快照 / 分析运行：1 / 1 / 1

初始化包按固定表/列白名单生成，强制排除 API Key、聊天/附件、确认/审计、持仓与账户。完整私有备份继续仅保存在 gitignored 的 `datavolume/`。

## 五、验证

- 服务端 TypeScript 类型检查通过。
- Vue TypeScript 类型检查通过。
- 迁移与数据库对账通过：版本 14，内容 27/29，7 个重复路径和证据均为 0。
- Agent 测试覆盖迁移证据表隐藏、单一事实源、目标日计划边界、领域写入语义、实盘例外和资金摘要。
- 完整测试 12 个文件、84 项全部通过；Vue 生产构建通过。
- 全新目录与空数据库从最终初始化包恢复后：内容文档 27、作业 4、长线估值 `success`、服务内 `buy_hold` 回测 `success`、新建会话返回 201、Agent 注册 12 个工具且 `fetch_market_data` 含 `financial_requests`。
- clean-room 服务的 `/`、`/content`、`/jobs`、`/data` 与 `/legacy/` 均返回 200；全局悬浮聊天、侧栏、拖拽/键盘缩放和宽度持久化由同一构建源码契约与既有交互回归覆盖。应用内浏览器在服务首次未启动时进入连接错误页，后续被其 URL 安全策略禁止重新导航，因此本轮未追加新的可视化截图，不把该工具限制记作产品失败。
- clean-room 临时服务、临时数据库和临时目录均已清理；8787、8788 和验收端口 18879 最终均无监听。
