# AGENTS 工作规范

## 独立边界

- 本仓库必须脱离任何父目录独立构建、测试、部署和恢复；生产代码不得读取或回写仓库外交易文件。
- PostgreSQL 是运行时唯一事实源。部署或运维任务必须先完整阅读 `docs/独立部署说明.md`。
- 新增项目产出只写在本仓库内；spec bundle 名和内容必须全部使用中文。

## 敏感信息

- API Key 只通过系统设置写入 PostgreSQL；不得写入 Git、环境模板、日志、URL、固定资产包或文档示例。
- `.env.local`、`datavolume/`、`server/uploads/`、数据库卷、构建产物和私有备份不得提交。
- 固定资产包只允许包含当前策略、策略演进摘要、定时任务定义与提示词；不得包含持仓、账户、流水、标的池、行情或运行历史。
- 提交前必须检查 `git status --ignored`、`git diff --cached` 和敏感模式扫描结果。

## 验证

- 代码变更至少运行相关类型检查与测试；Docker 部署变更还必须运行 `docker compose config` 和镜像构建。
- 验证后的临时测试文件如非必要直接删除，不保留重复测试资产。
