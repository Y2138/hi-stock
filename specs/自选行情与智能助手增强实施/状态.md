# 自选行情与智能助手增强实施

- 模式：治理
- 状态：已完成
- 更新：2026-08-19，实施与集成验证完成

## 目标与验收

- 目标：按《自选行情与智能助手增强技术设计评审说明》完成数据库、服务端、前端、Agent 与恢复链路改造。
- 验收：迁移可在隔离数据库执行；目录、自选、板块、近实时、指标、市场结构和标注接口闭环；Agent 遥测与受控工具满足安全边界；类型检查、永久测试和生产构建通过。
- 不可变约束：PostgreSQL 是 `project/` 唯一业务事实源；不读取或回写 `project/` 外交易文件；盘中采样不得冒充分钟 K 或写入 `market_bar`；策略发布只能由真实用户确认；Web 研究未确认供应商前不得注册或保存凭据；用户现有改动必须保留；临时验证文件验证后删除。

## 已确认决策

- 用户于 2026-08-19 要求“实施”，按已提交评审稿的推荐项执行。
- 标的池导航只读兼容一版后移除，`pool_membership` 永久保留。
- 全部目录可检索和关注；完整行情首期覆盖股票、标准指数、板块和 ETF。
- 前台 60 秒、后台 5 分钟作为容量目标；盘中采样保留最近 30 个交易日。
- 首期画线只做水平线、趋势线和文字。
- Web 研究采用官方或白名单站点契约；供应商未确认前只保留接口，不注册工具。

## 待确认与阻塞

- 无。若真实扶摇权限或容量证据使已确认刷新目标不可达，停止固化实际周期并回到用户确认。

## 审批与风险

- 风险事件：只向前迁移会改变自选和市场数据结构；统一自选开始正式写入后不能无损回写旧表。
- 缓解措施：迁移只新增或事务搬迁；功能开关分链路切换；隔离数据库验证；不在本任务中连接或写入生产数据库。
- 所需批准：实施当前评审方案。
- 批准状态：已批准
- 批准证据：用户在本任务中明确要求“实施”。

## 执行任务

- [x] 任务一：新增 `0022`—`0028` 数据库迁移及恢复白名单
  - 依赖：无
  - 负责人：当前模型
  - 写入范围：`project/server/migrations/`、`project/server/volume/`
  - 状态：已完成
  - 局部验证：`npm test -- tests/server/migrate.test.ts` 通过 2 项；`npm test -- tests/server/migration-volume.test.ts` 通过 4 项。
  - 结果：七个只向前迁移可幂等执行；初始化包 v3 排除临时/敏感表，恢复后创建指标脏标记。
- [x] 任务二：实现标的目录、板块、特色数据与扶摇统一请求队列
  - 依赖：任务一
  - 负责人：当前模型
  - 写入范围：`project/server/datasource/`、`project/server/scheduler/`
  - 状态：已完成
  - 局部验证：`npm run typecheck` 通过；`npm test -- tests/server/datasource.test.ts` 通过 22 项；`npm test -- tests/server/scheduler.test.ts` 通过 17 项。
  - 结果：扶摇请求统一为单并发四级公平队列；目录、交易日、四类板块、成分和七类市场结构数据均有严格解析与幂等落库；三个新增 pipeline 使用独立市场域锁并作为默认关闭的内置作业提供。
- [x] 任务三：实现统一自选、市场查询、近实时轮询、指标与标注领域服务
  - 依赖：任务一、任务二
  - 负责人：当前模型
  - 写入范围：`project/server/modules/`、`project/server/market-runtime/`、`project/server/indicators/`、`project/server/index.ts`、`project/server/http/`
  - 状态：已完成
  - 局部验证：`npm test -- tests/server/positions-watchlist.test.ts` 通过 15 项；`npm test -- tests/server/datasource.test.ts` 通过 22 项；服务端类型检查通过。
  - 结果：统一自选 entry/membership 模型、目录与板块查询、近实时 latest/series/SSE、指标脏标记与工作器、市场结构和乐观并发图表标注均经领域 service 闭环；盘中采样独立存储，未写入 `market_bar`。
- [x] 任务四：实现 Agent 遥测、候选只读工具与 Web 安全契约
  - 依赖：任务一、任务三
  - 负责人：当前模型
  - 写入范围：`project/server/agent/`
  - 状态：已完成
  - 局部验证：`npm test -- tests/server/chat-tools.test.ts` 通过 13 项；`npm test -- tests/server/chat-routes.test.ts` 通过 19 项。
  - 结果：Agent 运行与工具调用遥测只保存 token、成本、时延、大小、状态等元数据；五个市场领域只读工具默认关闭、按数据库开关逐次重验且不返回 `source_payload`；Web 研究仅保留白名单 Provider 契约，未注册工具或保存凭据。
- [x] 任务五：实现自选、行情、市场结构和图表标注前端
  - 依赖：任务三
  - 负责人：当前模型
  - 写入范围：`project/web/`
  - 状态：已完成
  - 局部验证：`npm run web:typecheck` 与 `npm run web:build` 通过；真实浏览器在 1280×720 和 900×720 下完成自选、行情、历史 K 线与设置页检查，无主布局横向溢出，控制台无错误。
  - 结果：新增自选导航和分组管理、近实时对比与历史 K 线、市场结构、MA/MACD、水平线/趋势线/文字标注、Agent 能力开关及运行摘要；URL 保留行情页签、标的和对比状态；旧标的池明确为只读兼容入口。
- [x] 任务六：完成永久测试、集成验证与临时文件清理
  - 依赖：任务一至任务五
  - 负责人：当前模型
  - 写入范围：`project/tests/`、必要配置与状态账本
  - 状态：已完成
  - 局部验证：`npm test` 通过 13 个文件、124 项；`npm run typecheck`、`npm run web:typecheck`、`npm run web:build`、`git diff --check -- project` 全部通过。
  - 结果：永久测试扩展在既有文件中；修正安全初始化包测试的迁移上限为 `0028`；修复会话事件 `BIGINT` 游标被文本别名按字典序排序、跨位数时漏回放的运行时缺陷，并保留跨位数回归覆盖；经哈希确认的旧最小 PNG 测试产物已删除。

## 集成验证

- 状态：通过
- 数据库与恢复：`0022`—`0028` 迁移幂等测试 2 项、初始化包与数据卷迁移测试 4 项、数据卷 HTTP 路由测试 4 项通过；所有操作均使用隔离测试库，未连接或写入生产数据库。
- 服务端与 Agent：重点六套件 73 项通过；完整测试 13 个文件、124 项全部通过；服务端 TypeScript 类型检查通过。
- 前端：Vue 类型检查与生产构建通过；1280×720、900×720 浏览器关键路径无横向溢出，控制台无错误。构建保留单包超过 500 kB 的 Vite 性能提示，不影响正确性验收。
- 安全边界：未启用 Web 工具，未保存 Web 凭据；领域工具默认关闭；不暴露 `source_payload`；盘中样本不写入 `market_bar`；策略发布确认边界未改变。
- 改动与清理：`git diff --check -- project` 通过；任务新增产出均位于 `project/`，项目外既有用户改动未触碰；临时测试上传已清理。

## 下一步

实施目标已完成。若进入真实环境上线，应由用户按既有备份与发布流程安排生产迁移、逐项开启市场作业及 Agent 市场工具开关；本次实施未替用户执行生产写入或启用开关。
